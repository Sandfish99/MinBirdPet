# v1.4.0 · 设置中心 + 番茄钟

## 新功能

- **设置中心**：右键菜单 →「设置」打开独立设置窗口
  - 搜索框 + 分类（宠物/行为/番茄钟/外观/高级）
  - 点击穿透、锁定位置、勿扰模式、气泡深浅主题等新设置项
  - 全部设置即时生效；支持导入/导出配置 JSON、恢复默认值
- **番茄钟**：右键菜单 →「番茄钟：开始专注」，菜单显示倒计时，
  支持暂停/继续/跳过/重置；时长与长休间隔在设置中心配置；
  休眠唤醒后计时正确恢复；阶段切换有气泡提醒
- **兼容性**：Per-Monitor DPI V2、多显示器与热插拔跟随、
  全屏自动躲藏、远程桌面/虚拟机自动降帧

## 安装说明

1. 下载 `MinBirdPet.exe`（约 42 MB，单文件绿色软件）
2. SHA-256：`__SHA256__`
3. 放到任意你喜欢的目录（例如 `D:\Tools\`），双击运行 —— 无需安装、无需管理员权限
4. 珉鸟会出现在任务栏上方；右键它即可打开菜单（设置/番茄钟/余额/天气）
5. 想开机自启：右键 → 勾选「开机自启」（写入当前用户注册表，无需管理员）

卸载：退出珉鸟 → 删除 exe → 如需清空数据，删除 `%APPDATA%\MinBirdPet` 文件夹。

## 更新 / 回滚

- 下载新版 `MinBirdPet_new.exe` 放到旧版同目录，重启珉鸟即自动完成更新（旧版保留为 `MinBirdPet_prev.exe`）
- 新版有问题？运行 `MinBirdPet.exe --rollback` 一键回退

## 要求

- Windows 10 1809+ / Windows 11（x64）
- WebView2 运行时（Win11 自带；Win10 缺失时安装一次即可）

> 非官方粉丝作品，与 aespa / SM Entertainment 无关；珉鸟形象版权归原作者及相关权利人所有。
